# Database - MySQL
DATABASE_URL="mysql://nhsortag_soro:mOE7iv5IartmDN8plocalhost:3306/nhsortag_sorokid"

# NextAuth
NEXTAUTH_URL="https://sorokid.com"
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Node environment
NODE_ENV="production"

# Server configuration
PORT=3000
HTTPS_PORT=443
HOSTNAME="0.0.0.0"

# SSL Configuration (optional - for custom HTTPS server)
# Leave empty if using reverse proxy (cPanel/Nginx handles SSL)
SSL_KEY_PATH=""
SSL_CERT_PATH=""
SSL_CA_PATH=""

# Application path (for PM2 on Linux)
APP_PATH="/var/www/sorokid"
